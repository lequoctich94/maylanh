function getIdDetailProduct(product_id, product_detail_id) {
    console.log('vd');
}

function getImageDetailProduct(product_id, product_detail) {
    return  product_id + "&" + product_detail.color.color_id + "&1";
}

